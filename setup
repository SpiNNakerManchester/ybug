set -a

YBUG_DIRS=$PWD
YBUG_PATH=$YBUG_DIRS/boot

#-------------------------------------------------------------------------------
# Add the directory to the Unix PATH and to the Perl library search path.

PATH=$YBUG_DIRS:$PATH
PERL5LIB=$YBUG_DIRS:$PERL5LIB

